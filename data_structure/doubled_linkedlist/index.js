class Node {
  constructor(data) {
    this.data = data;
    this.next = null;
  }
}

class LinkedList {
  constructor() {
    this.head = null;
    this.tail = null;
    this.size = 0;
  }

  /**
   *
   * @param {string} data
   * @returns {this}
   */
  insertFirst(data) {
    //
  }
}

const ll = new LinkedList();

console.log(ll);
